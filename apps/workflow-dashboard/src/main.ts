const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.innerHTML = `
    <main>
      <h1>Workflow Dashboard</h1>
      <p>Read-only local dashboard bootstrap in progress.</p>
    </main>
  `;
}
