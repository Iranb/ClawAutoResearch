{
  "id": "openclaw-research",
  "name": "OpenClaw Research",
  "description": "Multi-agent research workflow: ideation, experiment, analysis, paper, review.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "allowWorkspaceFallback": {
        "type": "boolean",
        "default": false,
        "description": "Allow research memory writes to fall back to workspace-level memory when OPENCLAW_PROJECT is unset."
      },
      "requireProjectIsolation": {
        "type": "boolean",
        "default": true,
        "description": "Require OPENCLAW_PROJECT for research memory writes."
      },
      "requireProjectIdInEntries": {
        "type": "boolean",
        "default": true,
        "description": "Require each structured memory entry to resolve to a project id."
      },
      "requireTrackId": {
        "type": "boolean",
        "default": true,
        "description": "Require track ids for idea and experiment memory writes."
      },
      "requireEvidencePointers": {
        "type": "boolean",
        "default": true,
        "description": "Require evidence pointers when writing structured research memory."
      },
      "reviewStateMaxAgeHours": {
        "type": "number",
        "minimum": 1,
        "default": 24,
        "description": "Maximum age of REVIEW_STATE.json before review resumability suggests a restart."
      },
      "projectsRoot": {
        "type": "string",
        "default": "~/.openclaw/projects",
        "description": "Default root directory used by research_memory and workflow state when resolving project-level paths."
      },
      "injectWorkflowContext": {
        "type": "boolean",
        "default": true,
        "description": "Inject a runtime workflow snapshot into each agent turn so roles, stages, blockers, and idle tasks stay visible."
      },
      "enforceWorkflowBoundaries": {
        "type": "boolean",
        "default": true,
        "description": "Block out-of-scope writes, unsafe spawns, and disallowed inter-agent messaging based on workflow role policy."
      },
      "blockDiscordAgentMentions": {
        "type": "boolean",
        "default": true,
        "description": "Sanitize raw @agent mentions in outbound Discord/chat messages and steer agents toward workflow-safe communication paths."
      },
      "enableWorkflowMailbox": {
        "type": "boolean",
        "default": true,
        "description": "Enable the research_workflow mailbox tool for bounded internal handoffs and blocker notes."
      },
      "heartbeatBackgroundChecks": {
        "type": "boolean",
        "default": true,
        "description": "On heartbeat/idling turns, inject bounded background-task guidance from the workflow state."
      },
      "maxWorkflowInboxMessages": {
        "type": "number",
        "minimum": 1,
        "default": 6,
        "description": "Maximum number of pending workflow mailbox items included in injected context or returned by default mailbox reads."
      },
      "agentContactCooldownSeconds": {
        "type": "number",
        "minimum": 0,
        "default": 300,
        "description": "Minimum cooldown between routing the same source agent to the same target agent through sessions_send, sessions_spawn, or workflow mailbox."
      },
      "enableChannelProjectBindings": {
        "type": "boolean",
        "default": false,
        "description": "Allow Discord/session channels to bind to project roots so different channels can run different research projects in the same OpenClaw process."
      },
      "channelProjectBindingsPath": {
        "type": "string",
        "default": "",
        "description": "Optional shared JSON path for channel-project bindings. When empty, bindings default to {projectRoot}/.openclaw-research/channel-project-bindings.json when a project is known, otherwise they fall back to the current workspace."
      },
      "defaultConferenceTemplatePath": {
        "type": "string",
        "default": "",
        "description": "Optional default conference paper template path. When configured, conference writing mode copies this template into each project before Writer edits it."
      },
      "defaultJournalTemplatePath": {
        "type": "string",
        "default": "",
        "description": "Optional default journal paper template path. When configured, journal writing mode copies this template into each project before Writer edits it."
      },
      "autoMode": {
        "type": "string",
        "enum": ["off", "conservative", "aggressive"],
        "default": "off",
        "description": "Workflow auto-dispatch mode. conservative auto-launches stage owners but still stops at GATE-5; aggressive also uses auto gate review to progress submit to done."
      },
      "autoGate": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "default": false
          },
          "allowAutonomousDone": {
            "type": "boolean",
            "default": true
          },
          "maxReviewRounds": {
            "type": "number",
            "minimum": 1,
            "default": 2
          },
          "maxMitigationRounds": {
            "type": "number",
            "minimum": 1,
            "default": 2
          },
          "reviewTimeoutMinutes": {
            "type": "number",
            "minimum": 1,
            "default": 20
          },
          "quorum": {
            "type": "number",
            "minimum": 1,
            "default": 2
          },
          "thresholds": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "review_to_write": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "avg": { "type": "number", "minimum": 0, "maximum": 10, "default": 7.8 },
                  "minSingle": { "type": "number", "minimum": 0, "maximum": 10, "default": 7.0 }
                }
              },
              "write_to_submit": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "avg": { "type": "number", "minimum": 0, "maximum": 10, "default": 8.2 },
                  "minSingle": { "type": "number", "minimum": 0, "maximum": 10, "default": 7.5 }
                }
              },
              "submit_to_done": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "avg": { "type": "number", "minimum": 0, "maximum": 10, "default": 8.8 },
                  "minSingle": { "type": "number", "minimum": 0, "maximum": 10, "default": 8.0 }
                }
              }
            }
          }
        }
      },
      "lobsterHandoff": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "default": false,
            "description": "Use the optional Lobster tool as the agent handoff backend for automatic workflow dispatches."
          },
          "autoModeOnly": {
            "type": "boolean",
            "default": true,
            "description": "Only use Lobster handoff when workflow auto mode is active."
          },
          "gatewayUrl": {
            "type": "string",
            "default": "http://127.0.0.1:18789",
            "description": "Gateway base URL used when invoking the optional Lobster tool from the research plugin."
          },
          "pipelinePath": {
            "type": "string",
            "default": "",
            "description": "Optional absolute path to a custom Lobster workflow file. When empty, the bundled workflow-agent-dispatch.lobster is used."
          },
          "timeoutMs": {
            "type": "number",
            "minimum": 1000,
            "default": 30000,
            "description": "Timeout passed to the Lobster tool when running the workflow handoff pipeline."
          },
          "maxStdoutBytes": {
            "type": "number",
            "minimum": 1024,
            "default": 512000,
            "description": "Maximum stdout bytes allowed for the Lobster tool run."
          },
          "fallbackToNative": {
            "type": "boolean",
            "default": true,
            "description": "If Lobster is unavailable or denied, fall back to the built-in native dispatch path instead of failing the workflow handoff."
          }
        }
      }
    }
  },
  "uiHints": {
    "allowWorkspaceFallback": {
      "label": "Allow Workspace Fallback"
    },
    "requireProjectIsolation": {
      "label": "Require Project Isolation"
    },
    "requireProjectIdInEntries": {
      "label": "Require Project ID"
    },
    "requireTrackId": {
      "label": "Require Track ID"
    },
    "requireEvidencePointers": {
      "label": "Require Evidence Pointers"
    },
    "reviewStateMaxAgeHours": {
      "label": "Review State Max Age (Hours)"
    },
    "projectsRoot": {
      "label": "Projects Root"
    },
    "injectWorkflowContext": {
      "label": "Inject Workflow Context"
    },
    "enforceWorkflowBoundaries": {
      "label": "Enforce Workflow Boundaries"
    },
    "blockDiscordAgentMentions": {
      "label": "Block Discord Agent Mentions"
    },
    "enableWorkflowMailbox": {
      "label": "Enable Workflow Mailbox"
    },
    "heartbeatBackgroundChecks": {
      "label": "Heartbeat Background Checks"
    },
    "maxWorkflowInboxMessages": {
      "label": "Workflow Inbox Size"
    },
    "agentContactCooldownSeconds": {
      "label": "Agent Contact Cooldown (Seconds)"
    },
    "enableChannelProjectBindings": {
      "label": "Enable Channel Project Bindings"
    },
    "channelProjectBindingsPath": {
      "label": "Channel Project Bindings Path"
    },
    "defaultConferenceTemplatePath": {
      "label": "Default Conference Template Path"
    },
    "defaultJournalTemplatePath": {
      "label": "Default Journal Template Path"
    },
    "autoMode": {
      "label": "Auto Mode"
    },
    "autoGate": {
      "label": "Auto Gate"
    },
    "lobsterHandoff": {
      "label": "Lobster Handoff"
    }
  },
  "skills": [
    "./skills/researcher",
    "./skills/reviewer",
    "./skills/orchestrator",
    "./skills/coder",
    "./skills/analyzer",
    "./skills/academic_writer",
    "./skills/cross-reviewer"
  ]
}
